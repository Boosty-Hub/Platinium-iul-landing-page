# ── Frontend (Vite) ──────────────────────────────────────────────
# La publishable key es PÚBLICA por diseño (va en el bundle del cliente).
# La seguridad real es RLS + least-privilege + guards, no esconder esta key.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxx

# ── RingCentral Embeddable softphone (Slice 3) ───────────────────
# Public OAuth Client ID for the RC Embeddable widget (browser-safe).
# Obtain from your RingCentral Developer Portal → App settings → Client ID.
# If absent/empty, the cockpit renders a "pending configuration" card instead
# of the RC widget, so the app never crashes without this value.
# VITE_RC_CLIENT_ID=

# ── Solo local (NO commitear — va en .env.local) ─────────────────
# Management API personal access token. Poder total sobre la cuenta.
# SUPABASE_ACCESS_TOKEN=sbp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# SUPABASE_PROJECT_REF=<project-ref>
